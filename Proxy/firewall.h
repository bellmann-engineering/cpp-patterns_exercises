#include <stdexcept>

#include "connectioninterface.h"
#include "networkconnection.h"

class Firewall : public ConnectionInterface
{
	// TODO: Set up a firewall that ensures that a connection 
	// can only be established through the allowed ports (e.g. 80 and 8080)
	
	

};
